SELECT z.Zone,
       SUM(a.total_amount) AS total_amount
FROM green_trips_data a
JOIN taxi_zones z ON a.PULocationID = z.LocationID
WHERE a.lpep_pickup_datetime BETWEEN '2025-11-18 00:00:00' AND '2025-11-18 23:59:59'
GROUP BY z.Zone
ORDER BY total_amount DESC
LIMIT 1;
